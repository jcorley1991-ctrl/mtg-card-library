import test from"node:test";import assert from"node:assert/strict";import{calculateOwnership}from"../src/core/decks.js";test("ownership distinguishes exact, alternate printing, and missing",()=>{const deck=[{name:"Card",cardId:"printing-a",oracleId:"oracle-1",finish:"nonfoil",language:"en",quantity:4}],collection=[{cardId:"printing-a",oracleId:"oracle-1",finish:"nonfoil",language:"en",quantity:1},{cardId:"printing-b",oracleId:"oracle-1",finish:"foil",language:"en",quantity:2}],[x]=calculateOwnership(deck,collection);assert.equal(x.exactOwned,1);assert.equal(x.alternateOwned,2);assert.equal(x.missing,1)});
